{% extends "admin.njk" %}

{% block content %}

<div class="container admin user">

  <form action="/admin/users?_method=DELTE">
    <input type="hidden" name="id" value="{{ user.id }}">
  </form>

  <form action="/admin/users" method="post">

    <div class="actions">
      <h2>Criar Usuário</h2>

      <div>
        <a onclick="submit()" class="button delete">Deletar</a>
        <button type="submit" class="button">Salvar</button>
      </div>
    </div>

    <div class="fields">
      {% include "user/fields.njk" %}
    </div>
  </form>

</div>

{% include "confirm.njk" %}

{% endblock content %}